# Testing Infrastructure

## Test Frameworks

**Unit/Integration:** None — no COBOL unit test framework (e.g., ZUTZCPC, COBOLcheck) detected
**E2E:** JCL batch execution with SAS report output comparison
**Coverage:** No coverage tooling detected

## Test Organization

**Location:** `TESTJCL` (single JCL job stream)
**Naming:** No test naming pattern — single monolithic test job
**Structure:** End-to-end black-box: submit bills → run pricer → compare SAS report output

## Testing Patterns

### End-to-End (Batch)

**Approach:** JCL submits a batch job that:
1. Deletes prior output datasets (IEFBR14 step)
2. Runs the pricer (HOSOP210) with test input files
3. Reads the output RATEFILE via SAS
4. Produces a formatted report (`PROC PRINT`) with key fields

**Input files (referenced in JCL, not in repo):**
- `HOSPV210.PRODBILL.INPUT` — test bill records
- `HOSPV210.PROVFILE` — provider reference data
- `HOSPV210.CBSA2021` — CBSA wage index data
- `HOSPV160.MSAH050` — legacy MSA wage index data

**Output validation:**
- SAS report includes: PROVIDER, FROM_DT, ADMT_DT, PAY_AMT_TOT, RTC, HIGH_DAYS, LOW_DAYS
- Manual comparison of expected vs actual payment amounts and return codes

### Unit Tests

**Not present.** No unit-level test infrastructure exists for individual COBOL paragraphs or calculations.

### Integration Tests

**Not present as separate tests.** The JCL batch test effectively serves as an integration test of all components together.

## Test Execution

**Commands:**
- Submit JCL: `SUB 'user.TESTJCL'` (TSO) or equivalent JES submission
- Review: Check `SYSOUT` and SAS report datasets

**Configuration:** JCL references user-specific dataset qualifiers (`K1P6.@BFN2699.*`)

## Coverage Targets

**Current:** Unknown — no measurement capability
**Goals:** None documented
**Enforcement:** None

## Test Coverage Matrix

| Code Layer                     | Required Test Type | Location Pattern | Run Command          |
| ------------------------------ | ------------------ | ---------------- | -------------------- |
| HOSDR210 (driver/lookups)      | none               | -                | -                    |
| HOSPR210 (pricer calculations) | none               | -                | -                    |
| HOSPRATE (rate constants)      | none               | -                | -                    |
| End-to-end pricer flow         | E2E (batch)        | TESTJCL          | Submit JCL via JES2  |

## Parallelism Assessment

| Test Type  | Parallel-Safe? | Isolation Model       | Evidence                                     |
| ---------- | -------------- | --------------------- | -------------------------------------------- |
| E2E (JCL)  | No             | Shared dataset names  | JCL uses fixed dataset qualifiers (K1P6.@BFN2699.*) |

## Gate Check Commands

| Gate Level | When to Use                | Command                          |
| ---------- | -------------------------- | -------------------------------- |
| Full       | After any code change      | Submit TESTJCL, review SAS report |

**Note:** No quick/unit-level gate check is possible with current infrastructure.

## Local Testing (GnuCOBOL)

**Environment:** GnuCOBOL 3.2 on Windows (`tools/gnucobol32/`)

**Test Programs (in `test/` directory):**
- `GENDATA.cbl` — Test data generator: produces `PROVFILE` (2 providers) and `BILLFILE` (40 test cases: TC01–TC36 + TC08B)
- `HOSOP210.cbl` — Local test driver: substitutes the production CICS wrapper; opens files, loads tables, invokes HOSDR210 for each bill
- `build-and-run.bat` — Automated build + execution script (7 steps)

**Build & Run:**
```
test\build-and-run.bat
```

Steps: (1) strip IBM line numbers from sources, (2) patch COPY statement for `.cpy`, (3–6) compile HOSPR210, HOSDR210, GENDATA, HOSOP210, (7) generate data and run pricer.

**Output:** `run/RATEFILE` — priced claim records (LINE SEQUENTIAL format, ~275 bytes per record due to trailing space trimming)

**Data Files (in `run/` directory):**
- `PROVFILE` — Generated by GENDATA (2 providers: 341234 Charlotte, 341235 NYC)
- `BILLFILE` — Generated by GENDATA (40 test bills)
- `CBSAFILE` — Copy of `CBSA2021` (7,478 CBSA wage index records)
- `MSAFILE` — Manual creation (1 record: MSA 6740, WI=1.0000 for pre-FY2006 cases)
- `RATEFILE` — Output from pricer execution

**Local Gate Check:**
```bash
test\build-and-run.bat
# Check RATEFILE output for expected RTC codes and payment amounts
```
